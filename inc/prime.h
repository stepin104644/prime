/**
* @file prime.h
*
*/
#ifndef __PRIME_H__
#define __PRIME_H__

/**
* Calculates whether the integer is a prime number or not
* @param[in] number to check if it a prime or not
* @return if a number is prime or not
* @note Returns -1 for negative values, zero and one
*/
int prime(int number);

#endif /* #ifndef __PRIME_H___H__ */
